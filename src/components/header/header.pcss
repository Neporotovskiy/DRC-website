:local {
    .header {
        height: 70px;
        background: #fff;
        box-shadow: 3px 3px 5px rgba(0, 0, 0, 0.1);
    }

    .content {
        width: 820px;
        height: 100%;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;

        .version {
            font-size: 14px;
            color: #bbb;
        }
    }
}
